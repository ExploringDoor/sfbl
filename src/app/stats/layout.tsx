import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Batting Stats — SFBL' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
