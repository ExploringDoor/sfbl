import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Schedule & Scores — SFBL' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
