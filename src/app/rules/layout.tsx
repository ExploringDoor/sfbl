import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Rules & Fields — SFBL' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
