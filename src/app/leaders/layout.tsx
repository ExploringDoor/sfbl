import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'League Leaders — SFBL' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
